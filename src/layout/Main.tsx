import React from 'react';
import Router from '../router/Router';
import './layout.css';

const Main = () => {
    return (
        <main>
            <Router />
        </main>
    );
}

export default Main;
